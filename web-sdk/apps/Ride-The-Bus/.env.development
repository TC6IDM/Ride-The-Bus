# Development counterpart of .env.production - see that file for why these
# exist. Same values apart from the site mode label; Chromatic stays off
# because the local dev server is not running visual regression.
PUBLIC_SITE_MODE=development
PUBLIC_SENTRY_DSN=
PUBLIC_CHROMATIC=false
